# Frappe VPS Setup Configuration

# Server Configuration (MANDATORY)
server:
  ip_address: "10.1.3.247"  # REQUIRED: IP address of your VPS server
  timezone: "Asia/Kolkata"
  swap_size: "2G"  # https://help.ubuntu.com/community/SwapFaq

# User Configuration
user:
  username: "frappeinstaller"  # Default username (optional, defaults to root)
  password: ""  # Will be generated if empty

# Database Configuration
database:
  mariadb_version: "10.11"  # MariaDB version to install
  root_password: ""  # Will be generated if empty

# Frappe/ERPNext Configuration
frappe:
  username: "rfdev"  # Default frappe system user (optional, defaults to resilient)
  version: "develop"
  bench_name: "rfdev-bench"  # Name of the bench directory (optional, defaults to resilient-bench)

  # "A" record to IP is expected to be present!!
  site_name: "ratnaafin-dev.localhost"
  admin_password: ""  # Will be generated if empty
  install_erpnext: true

# SSL Configuration
ssl:
  enabled: true
  email: "admin@yoursite.com"
